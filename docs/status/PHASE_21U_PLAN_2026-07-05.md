# Phase 21U Plan - Validation Scaling and 1-Minute Provider Coverage Recovery

Status date: 2026-07-05

`PHASE_21U_PLAN_STATUS = READY`

## Scope

Phase 21U starts from the accepted Phase 21T partial-blocked runtime. This phase is a validation-scaling and 1-minute provider-coverage recovery phase only. It is not a deployment phase, not model promotion, and not model activation.

The evidence database remains `adaptive_market_decoder_evidence` with `AMD_DB_ROLE=evidence`. Test execution must remain isolated through explicit test database configuration or explicit local SQLite paths.

## Guardrails

- Do not activate models.
- Do not approve or route a challenger.
- Do not use broker execution, order routing, options, production WebSocket ingestion, black-box/self-learning claims, or profitability claims.
- Do not bypass freshness, stale-window, validation, calibration, sensitivity, review, or proposal gates.
- Do not write test fixtures to the evidence database.
- Do not expose `FMP_API_KEY`, store secrets in tracked files, or send API keys in query strings.
- Use live FMP only from runtime environment or ignored `.env.local`, with header authentication.

## Phase 21T Baseline

- Evidence DB: `adaptive_market_decoder_evidence`
- Fixture rows: `0`
- Active models: `0`
- Bars: `13,560`
- 1day/5min/15min coverage: `10` RTH dates
- 1min coverage: `6` RTH dates
- Features/candidates/labels: `13,560 / 16,725 / 2,209`
- Replay runs/simulated trades: `18 / 25,048`
- Sensitivity runs/scenarios: `12 / 474`
- Exports: `158`
- Proposal `proposal_c34d0341e050a30bcdd815ffc0b0fa70`: `REJECT_CHALLENGER`, readiness `BLOCK`

## Execution Plan

1. Strengthen evidence isolation before live diagnostics:
   - Confirm explicit temporary SQLite registry paths ignore ambient evidence `DATABASE_URL`.
   - Run repository isolation regression checks.
   - Run `make evidence-db-audit`, `make test-db-smoke`, and `make evidence-guard-test`.
   - Confirm fixture rows remain `0`, active models remain `0`, and no dirty evidence windows are unexplained.

2. Diagnose 1-minute FMP coverage:
   - Use bounded diagnostic requests for `SPY`, `QQQ`, `AAPL`, and `NVDA`.
   - Compare `1min`, `5min`, `15min`, and `1day` behavior across known-successful and missing RTH dates.
   - Record sanitized endpoint, HTTP/status, provider request IDs, row counts, earliest/latest timestamps, missing dates, parser drops, and source IDs.
   - Classify the blocker as one of the required Phase 21U blocker classes.

3. Recover coverage where code or request-shaping allows:
   - If the issue is client windowing, date handling, timezone filtering, ingestion bounds, or parser drops, fix it and rerun bounded ingestion.
   - If the issue is provider-depth behavior, do not fabricate bars; record provider-depth limitation and continue interval-specific readiness for 5min and 15min.

4. Profile and scale replay-aware validation:
   - Measure outcome dataset build, feature/candidate retrieval, evidence cube build, validation scoring, persistence, calibration, drift/report, comparison, proposal, and exports.
   - Remove avoidable per-candidate evidence rebuild work while preserving no-future-leakage and embargo behavior.
   - Add targeted regression coverage for validation scaling metadata.

5. Run interval-specific governance:
   - Keep 1min readiness separate from 5min and 15min readiness.
   - Allow 5min/15min diagnostics to proceed independently when 1min is provider-depth blocked.
   - Keep all challenger/proposal outcomes inactive unless every gate actually passes.

6. Produce artifacts and exports:
   - Coverage diagnostic.
   - Validation performance profile.
   - Interval readiness matrix.
   - Scaled governance review.
   - Phase 21T vs Phase 21U comparison.
   - Proposal packet and decision ledger.
   - Export records with hashes, source IDs, row counts, and sheet names where applicable.

7. Verify:
   - Run backend, repository, model-review, research-cycle, export, scheduler, FMP ingestion/seed, freshness, frontend, compile, diff, and secret-scan gates where available.
   - Rerun evidence DB audit after tests and confirm fixture rows remain `0` and active models remain `0`.

## Acceptance Criteria

Phase 21U can only be accepted if isolation is repaired, 1-minute provider behavior is diagnosed with live bounded evidence, downstream readiness reports distinguish interval-specific blockers, validation scaling is implemented and measured, governance artifacts are regenerated without activation, exports are produced with source IDs and hashes, tests pass, secret scans pass, fixture rows remain `0`, and active models remain `0`.
