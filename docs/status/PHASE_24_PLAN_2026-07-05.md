# Phase 24 Plan - Pre-Registered 15min Specialist Evidence Expansion

`PHASE_24_PLAN_STATUS = PRE_REGISTERED_EVIDENCE_EXPANSION_ONLY`

Phase 24 is a research-only expansion of the Phase 23 `15min` `ten_am_reversal_zone` TAKE/WATCH specialist clue. It is not a model activation, proposal approval, broker execution path, WebSocket production ingestion path, stale-gate bypass, profitability claim, or secret-handling change.

## Starting State

- Starting certification: `PHASE_23_STATUS = ACCEPTED_NO_ROBUST_FILTER`.
- Evidence database: `adaptive_market_decoder_evidence`.
- Required clean-state invariants before live work: fixture rows `0`, active models `0`, dirty windows `0`, isolated `TEST_DATABASE_URL`, and no tracked runtime secret files.
- Phase 23 registered filter clue: interval `15min`, time bucket `ten_am_reversal_zone`, persisted non-active TAKE/WATCH score action, chronological split with at least a 60-minute embargo.
- Phase 23 blocker: full-grid sensitivity passed only for the TAKE/WATCH slice, but the slice had only 9 total candidates and 6 validation trades.

## Mandatory Read Notes

The Phase 24 brief, `AGENTS.md`, `README.md`, `docs/HANDOFF.md`, Phase 21S/T/V/W reports, Phase 22 reports, Phase 23 reports, live-data research-cycle results, evidence governance, replay-aware scoring, calibration, model review, proposal/champion governance, and the replay/FMP/artifact source paths were reviewed before data work.

Repository note: the brief names a `PHASE_21U_COMPLETION_2026-07-05.md` report, but this file is not present in the repo. The available Phase 21U artifact is `docs/status/PHASE_21U_PLAN_2026-07-05.md`; later Phase 21V and Phase 21W completion reports carry the post-repair runtime evidence.

## Execution Plan

1. Run evidence preflight against `adaptive_market_decoder_evidence`: role, fixture audit, active model count, dirty-window count, Phase 23 export/spec presence, ignored `.env.local`, and test database isolation smoke checks.
2. Confirm FMP capability review readiness for `historical_eod_full`, `intraday_1min`, `intraday_5min`, and `intraday_15min`.
3. Run a bounded dry-run seed plan for `SPY`, `QQQ`, `AAPL`, and `NVDA` over `1day`, `1min`, `5min`, and `15min`.
4. If capability review is `READY` and `FMP_API_KEY` is available only from runtime environment or ignored `.env.local`, run live FMP REST seed/incremental ingestion using header auth only.
5. Rebuild downstream artifacts from persisted real bars in order: features, candidate signals, labels, `candidate_market_replay`, `model_training_counterfactual`, freshness, and strict research-cycle dry-run with `allow_stale=false`.
6. Validate the pre-registered 15min TAKE/WATCH specialist filter with chronological discovery/validation split and at least 60-minute embargo.
7. Run full-grid sensitivity on the pre-registered slice and record validation, calibration, concentration, warnings, and sample-size blockers.
8. Run optional exploratory comparisons only after the pre-registered result is frozen; label them exploratory and keep them out of activation decisions.
9. Generate hashed exports and documentation: data expansion, artifact rebuild, filter spec, leakage/split, pre-registered validation, exploratory comparisons, sensitivity, specialist decision, Phase 23 vs Phase 24 comparison, completion, and a single output report pack.
10. Run tests and scans, then rerun the final evidence audit.

## Acceptance Rules

Phase 24 can only finish as an accepted evidence result if real expanded bars exist, downstream artifacts are rebuilt from those bars, freshness after-state is recorded, strict dry-run is recorded, exports have hashes and source IDs, tests pass, secret scans pass, and final evidence invariants remain clean.

If provider access or provider depth prevents expansion, the result must be `BLOCKED_PROVIDER`. If clean evidence invariants fail, the result must be `BLOCKED_CONTAMINATION`.
