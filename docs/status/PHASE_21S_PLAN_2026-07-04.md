# Phase 21S Plan - 2026-07-04

`PHASE_21S_PLAN_STATUS = SUPERSEDED_BY_ACCEPTED_COMPLETION`

Phase 21S is a clean evidence-store restoration/regeneration and governance-evidence recertification phase. It is not a model-improvement phase, not a model-activation phase, not a governance-gate loosening phase, and not a trading-execution phase.

## Guardrails

- Do not use contaminated evidence DB rows as approval evidence.
- Do not delete or destroy the contaminated evidence DB unless an operator explicitly approves deletion after archive.
- Do not activate, approve, or deploy the rejected challenger.
- Do not bypass stale gates or run `allow_stale=true` for certification.
- Do not add broker execution, order routing, options, gamma, Greeks, IV, market internals, Level 2, dark-pool data, order-book data, production WebSocket ingestion, black-box ML, or self-learning behavior.
- Do not claim profitability.
- Do not expose `FMP_API_KEY`, database credentials, or secret-like values.
- Do not place API keys in query strings.

## Planned Workflow

1. Archive/quarantine the contaminated `adaptive_market_decoder` evidence DB by recording database identity, Alembic revision, row counts, fixture-like row counts, and a contaminated archive dump/hash when practical.
2. Create a clean evidence database named `adaptive_market_decoder_evidence`, independent of `adaptive_market_decoder_test`.
3. Migrate the clean evidence DB to Alembic head with `AMD_DB_ROLE=evidence`.
4. Audit the clean evidence DB and verify zero fixture-like rows before any regeneration.
5. Check for clean restoration sources in this order:
   - clean Postgres backup/dump;
   - populated clean SQLite source;
   - runtime-only live FMP regeneration when `FMP_API_KEY` is present in environment or ignored local env.
6. If a clean source exists, restore or regenerate live bars, quote snapshots, features, candidates, labels, replay runs, model/review/proposal evidence, export metadata, and sensitivity status from that clean source only.
7. If no clean source exists, preserve the clean empty evidence DB and report `BLOCKED_NO_DATA`.
8. Re-run evidence/test isolation checks so mutating Postgres tests remain on `TEST_DATABASE_URL` and do not contaminate the clean evidence DB.
9. Run practical backend, frontend, database, compile, diff, and secret-scan checks.
10. Write Phase 21S completion, archive, clean DB, regeneration, and final governance certification reports.

## Acceptance Conditions

`PHASE_21S_STATUS = ACCEPTED` is allowed only if:

- contaminated evidence is archived or exact archive blocker is documented;
- a clean evidence DB exists and is migrated to Alembic head;
- the clean evidence DB has zero fixture-like rows before and after tests;
- live-data evidence is restored or regenerated from a clean source;
- bars, quote snapshots, downstream artifacts, governance records, exports, and sensitivity status are resolved;
- challenger rejection remains based on clean evidence only;
- the challenger remains inactive;
- tests and secret scans pass.

If the clean DB exists but no clean data source or runtime FMP key is available, the correct status is `PHASE_21S_STATUS = BLOCKED_NO_DATA`.

## Outcome

The planned live-FMP regeneration path was later completed through ignored `.env.local`. Final status is `PHASE_21S_STATUS = ACCEPTED`; see `docs/status/PHASE_21S_COMPLETION_2026-07-04.md`.
