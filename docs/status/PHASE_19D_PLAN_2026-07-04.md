# Phase 19D Runtime FMP Data Regeneration Plan - 2026-07-04

Phase 19D is a runtime data-regeneration and artifact-readiness certification pass on top of the repaired Phase 19C runtime. It is not a feature expansion, model activation, broker integration, profitability claim, WebSocket production-ingestion pass, or stale-gate bypass.

## Scope

- Load `FMP_API_KEY` only from the runtime environment or ignored `.env.local`.
- Verify the FMP REST client uses header authentication and strips query-string API keys.
- Run reviewed FMP REST endpoint entitlement checks for `quote`, `quote_short`, `batch_quote`, `batch_quote_short`, `historical_eod_full`, `intraday_1min`, `intraday_5min`, and `intraday_15min`.
- Seed SPY, QQQ, AAPL, and NVDA over `1day`, `1min`, `5min`, and `15min` for `2026-07-01T13:30:00+00:00` through `2026-07-02T19:59:00+00:00`.
- Rebuild features from persisted real bars, candidate signals from rebuilt features, labels from bars/features/candidates, `candidate_market_replay`, and `model_training_counterfactual` replay.
- Record freshness after-state and a strict research-cycle dry-run with `allow_stale=false`.
- Generate exports with export IDs, file hashes, and source IDs.
- Run tests and secret scans without exposing secrets.

## Constraints

- No model activation.
- No broker execution or order routing.
- No production WebSocket ingestion.
- No stale-gate bypass.
- No profitability claims.
- No secret values in commands, docs, exports, provider metadata, database rows, logs, or frontend bundles.

## Acceptance

`PHASE_19_STATUS = ACCEPTED` only if real bars exist, downstream artifacts are rebuilt from those bars, freshness after-state is recorded, strict research-cycle dry-run is recorded, exports are generated with hashes/source IDs, tests pass, and secret scans pass.

If provider entitlement or data availability blocks the run, the status is `BLOCKED_PROVIDER`. If rebuilds fail after real data lands, the status is `BLOCKED_REBUILD`. If strict dry-run blocks after successful data/rebuilds, the status is `PARTIAL_BLOCKED`.
