{
  "name": "adaptive-market-decoder",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": "24.18.0"
  },
  "scripts": {
    "dev": "corepack pnpm --filter @amd/web dev",
    "build": "corepack pnpm --filter @amd/shared build && corepack pnpm --filter @amd/web build",
    "check": "corepack pnpm --filter @amd/shared check && corepack pnpm --filter @amd/web check",
    "test": "corepack pnpm --filter @amd/shared test && corepack pnpm --filter @amd/web test",
    "lint": "corepack pnpm --filter @amd/web lint"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1",
    "prettier": "^3.6.2"
  }
}
